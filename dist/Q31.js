"use strict";
let userName1 = ["Ali", "Ahmed", "Saim", "Khan", "Jhon"];
if (userName1.length === 0) {
    console.log(`We need to find some users!`);
}
else {
    userName1 = [];
    console.log(`All users have been removed.` + userName1.length);
}
